# NOTE: This is a template. Create actual secrets using:
# kubectl create secret generic postgres-secret --from-literal=password=YOUR_PASSWORD -n routing-dispatch
# kubectl create secret generic app-secrets --from-literal=jwt-secret=YOUR_JWT_SECRET -n routing-dispatch

apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: routing-dispatch
type: Opaque
stringData:
  password: CHANGE_ME_IN_PRODUCTION
---
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: routing-dispatch
type: Opaque
stringData:
  jwt-secret: CHANGE_ME_IN_PRODUCTION
